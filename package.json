{
  "name": "paper-shaders",
  "private": true,
  "version": "0.0.0",
  "license": "MIT",
  "workspaces": [
    "packages/shaders",
    "packages/shaders-react"
  ],
  "scripts": {
    "build": "bun run build.js",
    "publish-all": "bun run build && bun run publish.js",
    "publish-all-test": "bun run build && bun run publish.js --dry-run"
  },
  "dependencies": {
    "@types/bun": "^1.1.11"
  }
}
