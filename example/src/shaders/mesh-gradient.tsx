import { MeshGradient } from '@paper-design/shaders-react';
import * as React from 'react';

export const MeshGradientExample = () => {
  return <MeshGradient speed={0.25} style={{ position: 'fixed', width: '100%', height: '100%' }} />;
};
